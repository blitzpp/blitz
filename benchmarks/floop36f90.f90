
      SUBROUTINE floop36_F90(N, x, e)
      INTEGER i, N
      REAL*4 x(N), e(N)

      x = exp(e)
      RETURN
      END


      SUBROUTINE floop36_F90Overhead(N, x, e)
      INTEGER i, N
      REAL*4 x(N), e(N)

      RETURN
      END
